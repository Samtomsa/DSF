6. Reuse the code from the tutorial to run the relevant part (training) on Ubelix. Document how
you do the code modification and transfer.


#!/bin/bash
#SBATCH --job-name="Midterm Project"
#SBATCH --time=00:60:00
#SBATCH --mem-per-cpu=2GB

module purge
module load Python

srun python3 ./DSF/midtermcode.py

to run the training you need that dataset, so best think would be to download the data file and but it in the DSF folder inside 
